# 01-03 Plan: Co-Writer UI Parity

## Goal

Port/adapt the upstream Co-Writer document workflow into ZAKI Learn without introducing a new dashboard wrapper.

## Requirements

- LEARN-PARITY-006
- LEARN-UX-001 through LEARN-UX-007
- LEARN-SEC-005

## Upstream Truth

- `/Users/nova/Desktop/zaki-learning-engine/web/app/(workspace)/co-writer/page.tsx`
- `/Users/nova/Desktop/zaki-learning-engine/web/app/(workspace)/co-writer/[docId]/page.tsx`
- `/Users/nova/Desktop/zaki-learning-engine/deeptutor/api/routers/co_writer.py` if present

## ZAKI Files

- `src/app/components/learning/LearningPage.tsx`
- `src/lib/learningApi.ts`
- `backend/src/index.js`

## Implementation Steps

1. Compare upstream Co-Writer list and document detail flows to ZAKI `WriterPanel`.
2. Patch missing list/detail/create/template/edit/save/delete affordances.
3. Confirm co-writer mutation payloads cannot include operator-managed provider/model fields.
4. Browser-verify `/learn?view=writer` and `/learn?view=co-writer`.
5. Run `npm run typecheck`.
6. Run targeted tests when behavior changes.
7. Update `01-03-SUMMARY.md`.

## Acceptance Criteria

- Co-Writer route opens the upstream-shaped document workflow.
- Empty state, create flow, template flow, save/update/delete flow are visible or intentionally gated.
- No DeepTutor branding appears.
- Provider/model routing remains operator-managed.
