# 01-04 Plan: Space UI Parity

## Goal

Port/adapt the upstream Space page sections into ZAKI Learn without wrapping them in a dashboard or changing the meaning of the main ZAKI shell routes.

## Requirements

- LEARN-PARITY-004
- LEARN-PARITY-008
- LEARN-UX-001 through LEARN-UX-007
- LEARN-SEC-005

## Upstream Truth

- `/Users/nova/Desktop/zaki-learning-engine/web/app/(workspace)/space/page.tsx` if present
- `/Users/nova/Desktop/zaki-learning-engine/web/components/space/*`
- `/Users/nova/Desktop/zaki-learning-engine/deeptutor/api/routers/*` for memory, skills, notebooks, and user space capabilities

## ZAKI Files

- `src/app/components/learning/LearningPage.tsx`
- `src/lib/learningApi.ts`
- `backend/src/index.js`

## Implementation Steps

1. Locate upstream Space page/component truth.
2. Compare upstream sections to current ZAKI `LearningSpacePanel`.
3. Patch missing user-managed memory, skill, notebook, question, and linked context sections.
4. Keep provider/model routing operator-managed.
5. Browser-verify `/learn?view=space`.
6. Run `npm run typecheck`.
7. Run targeted tests when behavior changes.
8. Update `01-04-SUMMARY.md`.

## Acceptance Criteria

- Space route opens an upstream-shaped Space surface inside ZAKI Learn.
- User-managed settings are available; operator-managed settings are absent.
- Existing main ZAKI categories remain untouched.
- No DeepTutor branding appears.
