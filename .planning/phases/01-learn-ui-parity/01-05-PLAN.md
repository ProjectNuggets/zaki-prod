# 01-05 Plan: TutorBot UI Parity

## Goal

Port/adapt upstream TutorBot management and chat surfaces into ZAKI Learn while preserving hosted auth, quota, and operator-managed provider/model routing.

## Requirements

- LEARN-PARITY-005
- LEARN-UX-001 through LEARN-UX-007
- LEARN-SEC-005

## Upstream Truth

- `/Users/nova/Desktop/zaki-learning-engine/web/app/(workspace)/agents/page.tsx`
- `/Users/nova/Desktop/zaki-learning-engine/web/app/(workspace)/agents/[botId]/chat/page.tsx`
- `/Users/nova/Desktop/zaki-learning-engine/deeptutor/api/routers/tutorbot.py` or agent router equivalents

## ZAKI Files

- `src/app/components/learning/LearningPage.tsx`
- `src/lib/learningApi.ts`
- `backend/src/index.js`

## Implementation Steps

1. Compare upstream TutorBot list/create/detail/chat flows to current ZAKI `AgentsPanel`.
2. Patch missing agent list, create, soul/channel schema, persona, destroy, and chat affordances.
3. Confirm WebSocket auth and prompt quota behavior stay aligned with BFF security rules.
4. Browser-verify `/learn?view=tutorbot` and `/learn?view=agents`.
5. Run `npm run typecheck`.
6. Run targeted tests when behavior changes.
7. Update `01-05-SUMMARY.md`.

## Acceptance Criteria

- TutorBot route opens upstream-shaped management and chat surfaces.
- User can create/select/destroy hosted tutor agents when backend is enabled.
- Chat surface uses ZAKI authenticated WebSocket routes only.
- Provider/model settings remain operator-managed.
- No DeepTutor branding appears.
