# 01-01 Plan: Route Parity Manifest And Browser Audit

## Goal

Create the execution baseline for page-by-page Learn parity by documenting all expected routes, upstream references, ZAKI components, current browser behavior, and first code mismatches.

## Requirements

- LEARN-PARITY-001 through LEARN-PARITY-010
- LEARN-UX-001 through LEARN-UX-007

## Files To Read

- `docs/zaki-learning-integration-spec.md`
- `src/app/components/learning/LearningPage.tsx`
- `src/app/components/learning/LearningBookWorkspace.tsx`
- `src/lib/learningApi.ts`
- `/Users/nova/Desktop/zaki-learning-engine/web/app/(workspace)/book/page.tsx`
- `/Users/nova/Desktop/zaki-learning-engine/web/components/knowledge/KnowledgePage.tsx`
- `/Users/nova/Desktop/zaki-learning-engine/web/app/(workspace)/co-writer/page.tsx`
- `/Users/nova/Desktop/zaki-learning-engine/web/app/(workspace)/agents/page.tsx`
- `/Users/nova/Desktop/zaki-learning-engine/web/components/space`

## Implementation Steps

1. Add `.planning/phases/01-learn-ui-parity/PARITY-MANIFEST.md`.
2. List every Learn view and alias:
   - chat
   - sources/knowledge
   - books/book
   - writer/co-writer
   - review/questions
   - space
   - agents/tutorbot
   - workspaces/solve/vision
   - research
   - quiz
   - visualize
   - math/mathematics/math-animator
3. For each route, record:
   - upstream source file
   - ZAKI component
   - current route status
   - browser verification signal
   - missing UI/wiring/security items
4. Patch missing route aliases if a view falls back to Chat incorrectly.
5. Run browser DOM verification for affected routes.
6. Run `npm run typecheck`.
7. Update `.planning/STATE.md` with completed plan status.

## Acceptance Criteria

- Manifest exists and points GSD to exact next pages.
- No known Learn route falls back to Chat unless Chat is intentionally the target.
- Existing Books route remains library-first.
- Typecheck passes.
- Browser verification confirms changed routes.

## Do Not

- Do not redesign pages.
- Do not remove upstream capabilities.
- Do not expose operator-managed settings to users.
- Do not change ZAKI primary navigation semantics.
