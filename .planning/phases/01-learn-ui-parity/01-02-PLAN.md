# 01-02 Plan: Sources / Knowledge UI Parity

## Goal

Port/adapt the upstream Knowledge page shape into ZAKI Learn without inventing a dashboard wrapper. The ZAKI route must feel like the upstream Knowledge surface inside the ZAKI shell and must preserve hosted upload safety.

## Requirements

- LEARN-PARITY-002
- LEARN-PARITY-010
- LEARN-UX-001 through LEARN-UX-007
- LEARN-SEC-007
- LEARN-SEC-009
- LEARN-SEC-010

## Upstream Truth

- `/Users/nova/Desktop/zaki-learning-engine/web/components/knowledge/KnowledgePage.tsx`
- `/Users/nova/Desktop/zaki-learning-engine/web/components/knowledge/KnowledgeBaseList.tsx`
- `/Users/nova/Desktop/zaki-learning-engine/web/components/knowledge/KnowledgeBaseDetail.tsx`
- `/Users/nova/Desktop/zaki-learning-engine/web/components/knowledge/KbFilesTab.tsx`
- `/Users/nova/Desktop/zaki-learning-engine/web/components/knowledge/FileDropZone.tsx`
- `/Users/nova/Desktop/zaki-learning-engine/web/components/knowledge/KbSettingsSection.tsx`
- `/Users/nova/Desktop/zaki-learning-engine/web/components/knowledge/KbUpdateHistory.tsx`

## ZAKI Files

- `src/app/components/learning/LearningPage.tsx`
- `src/lib/learningApi.ts`
- `backend/src/index.js`
- `backend/src/learning-upload-limits.js`

## Implementation Steps

1. Compare upstream Knowledge component structure to ZAKI `SourcesPanel`.
2. Patch ZAKI `SourcesPanel` so its layout matches upstream:
   - left knowledge-base list
   - selected knowledge-base detail
   - files tab
   - add documents tab
   - settings tab
   - empty states
   - progress/reindex/status affordances where backend exists
3. Preserve hosted transport rules:
   - file upload
   - image/document support
   - browser folder upload where available
   - archive upload if backend route exists
   - no server-local path linking
4. Confirm backend routes enforce upload limits before forwarding.
5. Add or update focused tests only if behavior changes.
6. Browser-verify:
   - `/learn?view=sources`
   - `/learn?view=knowledge`
   - create empty state
   - selected-KB state if local data exists
   - upload controls render without unsafe local path UI
7. Run `npm run typecheck`.
8. Code review against the spec and update `01-02-SUMMARY.md`.

## Acceptance Criteria

- `sources` and `knowledge` routes render the same ZAKI component.
- Knowledge page no longer looks like a generic ZAKI dashboard panel if upstream has a richer surface.
- Every upload control is hosted-safe.
- No DeepTutor branding appears.
- Typecheck passes.
- Browser verification is recorded in the summary.

## Review Checklist

- No direct learning-engine browser URL.
- No internal token exposure.
- No local filesystem path entry for hosted users.
- No raw provider/model settings exposed.
- No large unrelated refactor.
