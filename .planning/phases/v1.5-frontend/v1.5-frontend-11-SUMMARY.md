---
phase: v1.5-frontend
plan: 11
subsystem: i18n
tags: [i18n, voice, task-checklist, settings, localization]
dependency_graph:
  requires: [v1.5-frontend-03]
  provides: [input.voice.*, task.*, zakiSettingsSheet.fields.voiceReplies.requiresTelegram]
  affects: [InputArea, NullalisRuntimeWidgets, ZakiSettingsSheet]
tech_stack:
  added: []
  patterns: [react-i18next useTranslation, t() call replacement]
key_files:
  created: []
  modified:
    - src/i18n/locales/en.json
    - src/i18n/locales/ar.json
    - src/app/components/InputArea.tsx
    - src/app/components/chat/NullalisRuntimeWidgets.tsx
decisions:
  - Used input.voice.errorGeneric for all three voice failure toasts (blob empty, no speech detected, transcription exception) since all map to the same user-facing concept
  - TaskChecklist did not previously call useTranslation — added hook directly inside the component (other components in same file already had it via ApprovalRequiredCard)
  - ZakiSettingsSheet already had the t("zakiSettingsSheet.fields.voiceReplies.requiresTelegram") call at line 1110 from a prior commit; plan 11 only needed to add the JSON key
metrics:
  duration_minutes: 12
  completed_date: "2026-04-30"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 4
requirements: [REQ-V15-009]
---

# Phase v1.5 Plan 11: i18n Debt Closure Summary

i18n debt cleanup closing audit findings H9, L6, and L10. Voice recording strings and error toasts in InputArea, TaskChecklist labels in NullalisRuntimeWidgets, and the requiresTelegram helper key in ZakiSettingsSheet all now route through t() with full Arabic translations.

## Tasks Completed

| Task | Description | Commit |
|------|-------------|--------|
| 1 | Add i18n keys to en.json + ar.json | 72699e6 |
| 2 | Wire t() into InputArea, NullalisRuntimeWidgets, ZakiSettingsSheet | 98fae4b |

## What Was Built

**Task 1 — JSON keys added:**

- `input.voice.*` (7 keys): tapToRecord, recording, listening, stop, errorMicAccess, errorGeneric, errorNoBrowser — in both en + ar
- `task.*` (3 keys): plan, task, tasks — in both en + ar
- `zakiSettingsSheet.fields.voiceReplies.requiresTelegram` — in both en + ar

**Task 2 — Component wiring:**

- `InputArea.tsx`: 6 hardcoded voice strings replaced with t() calls covering the recording countdown label, cancel/stop button aria-labels, mic button aria-label (3 states), and 3 error toasts
- `NullalisRuntimeWidgets.tsx`: Added `useTranslation` hook to `TaskChecklist`; replaced "Task Plan" summary text and "task"/"tasks" aria-label pluralization with t() calls
- `ZakiSettingsSheet.tsx`: No code change needed — file already had the t() call; adding the JSON key closed H9

## Deviations from Plan

**1. [Rule 1 - Bug] TaskChecklist missing useTranslation hook**
- Found during: Task 2
- Issue: `TaskChecklist` component used `useTranslation` pattern (per plan) but the hook was not imported/called inside that function. The import was at file level but only consumed by `ApprovalRequiredCard`.
- Fix: Added `const { t } = useTranslation();` at the top of `TaskChecklist`. The variable name `t` shadowed the task parameter in `useState(() => tasks.some((t) => t.status === "running"))` — this pre-existed and is fine as they are different scopes.
- Files modified: src/app/components/chat/NullalisRuntimeWidgets.tsx
- Commit: 98fae4b

**2. [Note] ZakiSettingsSheet.tsx required no code change**
- The file already contained `t("zakiSettingsSheet.fields.voiceReplies.requiresTelegram")` at line 1110 from a previous plan. Plan 11 only needed to supply the JSON key. H9 is closed.

## Pre-existing Failing Test (Out of Scope)

`src/stores/zakiSessionUiStore.test.ts` — one test fails:

```
● zakiSessionUiStore › maps backend session truth into UI state
  expect(mapped.contextPressureState).toBe("warning")
  Received: "normal"
```

This failure pre-exists and is unrelated to plan 11 changes. Per plan instructions, not fixed here.

## Verification

- `npm run typecheck`: exit 0
- `npm run test`: 168/169 tests pass; 1 pre-existing failure in zakiSessionUiStore.test.ts (out of scope)
- JSON validation: both en.json and ar.json parse cleanly with all required keys present
- Grep checks: no hardcoded "Recording…", "Listening…", "Tap to record", "Microphone access denied", "Voice transcription failed" literals remain in InputArea.tsx

## Known Stubs

None. All wired keys resolve to real strings in both locales.

## Threat Flags

None. This plan modifies only i18n string routing — no new endpoints, auth paths, or data flows introduced.

## Self-Check: PASSED

- SUMMARY.md exists at .planning/phases/v1.5-frontend/v1.5-frontend-11-SUMMARY.md
- Commit 72699e6 found: i18n keys
- Commit 98fae4b found: t() wiring
