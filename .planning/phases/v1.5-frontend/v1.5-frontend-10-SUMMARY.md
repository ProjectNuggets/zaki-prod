---
phase: v1.5-frontend
plan: 10
subsystem: navigation
tags: [sidebar, dashboard, brain, navigation, i18n]
requires: [v1.5-frontend-06]
provides: [sidebar-brain-entry, dashboard-brain-card]
affects: [SidebarModeSwitch, ZakiDashboard]
tech-stack:
  added: []
  patterns: [CSS-only keyframe animation, TanStack Query for corpus count, useLocation for active nav state]
key-files:
  created: []
  modified:
    - src/app/components/sidebar/SidebarModeSwitch.tsx
    - src/app/components/chat/views/ZakiDashboard.tsx
    - src/i18n/locales/en.json
    - src/i18n/locales/ar.json
key-decisions:
  - Brain sidebar entry uses location.pathname === "/brain" for active highlight (consistent with Spaces/ZAKI pattern)
  - BrainNodeCluster uses inline @keyframes brainPulse (CSS only, no framer-motion)
  - useBrainGraph called with max_nodes:1 to keep payload tiny, only total_nodes_in_corpus used
  - Brain card placed after category pills but before expanded sub-suggestions (inside !activeCat block)
requirements-completed: [REQ-V15-002, REQ-V15-003]
duration: 12 min
completed: "2026-04-30"
---

# Phase v1.5 Plan 10: Sidebar Brain Entry + Dashboard Brain Card Summary

Added two clickable entry points to the `/brain` route: a Brain button in the sidebar navigation (SidebarModeSwitch) and a full-width Brain card on the ZakiDashboard with live memory count and CSS-only animated node cluster.

**Duration:** 12 min | **Tasks:** 2/2 | **Files changed:** 4

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Sidebar Brain entry (REQ-V15-002) | b2f3ba1 | SidebarModeSwitch.tsx |
| 2 | Dashboard Brain card (REQ-V15-003) | b2f3ba1 | ZakiDashboard.tsx, en.json, ar.json |

## What Was Built

**Task 1 — SidebarModeSwitch.tsx:**
- Added `Brain` icon import from lucide-react
- Added `useNavigate` and `useLocation` from react-router-dom
- Added Brain button after the Spaces button, matching the exact visual pattern (active indicator bar, selected bg, RTL support)
- Active state: `location.pathname === "/brain"` drives `bg-zaki-selected` and `text-zaki-primary`
- Uses `t("sidebar.brain")` i18n key (added in plan 06)

**Task 2 — ZakiDashboard.tsx:**
- Added `Brain` icon, `useNavigate`, `useBrainGraph` imports
- Calls `useBrainGraph(userId, { max_nodes: 1 })` and reads `total_nodes_in_corpus` for memory count
- Brain card renders inside `!activeCat` block (between category pills and memory import pill)
- `BrainNodeCluster` helper: 5 pulsing dots with staggered `brainPulse` CSS keyframe animation
- i18n keys added: `brain.dashboard.{title, memoryCount, memoryCountZero}` in both locales

## Deviations from Plan

None - plan executed exactly as written.

## Verification

- `npm run typecheck`: PASS (0 errors)
- `npm run test`: 168/169 pass. 1 pre-existing failure in `zakiSessionUiStore.test.ts` (contextPressureState mapping) unrelated to this plan's changes — present in git history before plan 10 execution.
- All acceptance criteria met for both tasks.

## Known Stubs

None. The Brain card wires real data via `useBrainGraph` — when `total_nodes_in_corpus` is 0 (no memories yet), it displays the `memoryCountZero` string.

## Self-Check: PASSED

- `src/app/components/sidebar/SidebarModeSwitch.tsx`: exists, contains `navigate("/brain")`, `sidebar.brain`, `Brain` import, active state check
- `src/app/components/chat/views/ZakiDashboard.tsx`: exists, contains `BrainNodeCluster`, `useBrainGraph`, `brain.dashboard.title`, `brainPulse`
- Commit `b2f3ba1` exists in git log
