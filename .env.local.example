# No frontend env vars needed — all nullalis traffic flows through the BFF.
# Set NULLALIS_DEV_USER_ID in backend/.env to bypass auth in local dev
# (legacy NULLCLAW_DEV_USER_ID still accepted; sunset 2026-05-15).
