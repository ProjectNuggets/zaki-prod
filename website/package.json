{
  "name": "@zaki/website",
  "private": true,
  "version": "0.3.0-launch-draft",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve-launch-draft.mjs",
    "test": "node scripts/check-launch-draft.mjs launch-draft",
    "typecheck": "node --check scripts/build-launch-draft.mjs && node --check scripts/check-launch-draft.mjs && node --check scripts/serve-launch-draft.mjs && node --check launch-draft/mascot.js && node --check launch-draft/game.js && node --check launch-draft/zee-run.js && node --check launch-draft/site/product.js && npm test",
    "build": "node scripts/build-launch-draft.mjs && node scripts/check-launch-draft.mjs dist"
  },
  "engines": {
    "node": ">=20"
  }
}
