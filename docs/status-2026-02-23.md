# ZAKI Prod-Branch Status — 2026-02-23

## Repo + Branch Context
- Repo: `zaki-prod`
- Active working branch: `payment-checkout`
- Base strategy: branch from prod, implement + validate, then PR/merge to prod.
- This is the cleanest way to move forward.

## Current State (What Is Done)

### 1) Billing/Checkout
- Pricing and billing flow code has been ported to this branch.
- Stripe provider logic is present in backend and frontend.
- UI lock state was caused by empty Stripe env vars (not missing code).
- After env update, checkout is working locally.

### 2) Memory End-Session (Real Fix)
- Placeholder `memory-legacy` path has been removed.
- New real session processor added:
  - `backend/src/memory/session-summary.js`
  - Uses real extraction + store + conflict pipeline.
- Endpoint wiring cleaned:
  - `backend/src/memory/session-end-route.js`
  - `/api/memory/end-session` now uses this handler.
- Added safety caps for stability:
  - max user messages scanned per session
  - max facts per message
  - max total facts per session

### 3) Test Coverage Added (Code-Level)
- New tests added:
  - `backend/src/memory/session-summary.test.js`
  - `backend/src/memory/session-end-route.test.js`
- Coverage includes:
  - short-session skip
  - successful queue path
  - persistence behavior
  - duplicate suppression
  - contradiction -> conflict
  - error resilience and caps

### 4) Release Smoke Automation
- Added API-level smoke script:
  - `scripts/smoke-memory-session.mjs`
- Added npm command:
  - `npm run smoke:memory-session`
- README updated with run instructions.

## Runtime Dependencies (Must Be Set)

### Stripe (required for checkout unlock)
- `STRIPE_SECRET_KEY`
- `STRIPE_WEBHOOK_SECRET`
- `STRIPE_PRICE_STUDENT`
- `STRIPE_PRICE_PERSONAL`
- `ZAKI_BILLING_PROVIDER=stripe`

### Memory session end processing
- `ZAKI_ENABLE_SESSION_SUMMARIZATION=true`
- Optional tuning:
  - `ZAKI_SESSION_MEMORY_MAX_USER_MESSAGES=8`
  - `ZAKI_SESSION_MEMORY_MAX_FACTS_PER_MESSAGE=8`
  - `ZAKI_SESSION_MEMORY_MAX_TOTAL_FACTS=64`

## What To Run Before Merge/Release

1. Backend tests
- `npm --prefix backend test`

2. Frontend tests
- `npm test -- --runInBand`

3. Memory API smoke
- `SMOKE_BASE_URL=<api_url> SMOKE_USER_EMAIL=<verified_user> SMOKE_USER_PASSWORD='<pwd>' npm run smoke:memory-session`

4. Billing sanity
- Login -> Pricing -> checkout redirect works.
- Post-payment status refresh works.
- Already-subscribed guard blocks duplicate subscription purchase attempts.

## Next Steps (Priority)

### P0 (before next release)
1. Finalize Stripe production webhook verification and run one live end-to-end billing test.
2. Add billing webhook failure visibility (admin/internal view or logs with clear status counters).
3. Add retry/reconciliation for any delayed billing state sync edge cases.
4. Run full memory smoke + manual contradiction checks on staging/prod-like env.

### P1 (next major user-facing increment)
1. Image generation MVP (provider adapter + entitlement gate + rate/cost limits).
2. Search mode finalization (explicit mode UX + backend contract tests).
3. Additional release smoke script for billing end-to-end (checkout -> webhook -> entitlements).

### P2
1. Internal analytics dashboard (super-admin only).
2. Funnel and retention instrumentation.

## Operational Notes
- Keep working from `payment-checkout` until validations pass.
- Open PR to prod only after env parity + smoke checks are green.
- Do not rely on UI state alone for billing readiness; always confirm `/api/billing/config` + webhook events.
