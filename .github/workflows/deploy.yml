name: Build and Deploy ZAKI

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  DO_REGISTRY: registry.digitalocean.com/alis24-registry
  GHCR_REGISTRY: ghcr.io/projectnova

jobs:
  build-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Login to DO Registry
        uses: docker/login-action@v3
        with:
          registry: registry.digitalocean.com
          username: ${{ secrets.DO_REGISTRY_TOKEN }}
          password: ${{ secrets.DO_REGISTRY_TOKEN }}
      
      - name: Build and push backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ${{ env.DO_REGISTRY }}/zaki-api:latest
            ${{ env.DO_REGISTRY }}/zaki-api:${{ github.sha }}
          platforms: linux/amd64

  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Login to DO Registry
        uses: docker/login-action@v3
        with:
          registry: registry.digitalocean.com
          username: ${{ secrets.DO_REGISTRY_TOKEN }}
          password: ${{ secrets.DO_REGISTRY_TOKEN }}
      
      - name: Build and push frontend
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.DO_REGISTRY }}/zaki-web:latest
            ${{ env.DO_REGISTRY }}/zaki-web:${{ github.sha }}
          platforms: linux/amd64
          build-args: |
            VITE_ZAKI_BACKEND_URL=https://api.chatzaki.com

  build-website:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Build and push website
        uses: docker/build-push-action@v5
        with:
          context: ./website
          file: ./website/Dockerfile
          push: true
          tags: |
            ${{ env.GHCR_REGISTRY }}/zaki-website:latest
            ${{ env.GHCR_REGISTRY }}/zaki-website:${{ github.sha }}
          platforms: linux/amd64

  deploy:
    needs: [build-backend, build-frontend, build-website]
    runs-on: ubuntu-latest
    steps:
      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DO_API_TOKEN }}
      
      - name: Configure kubectl
        run: doctl kubernetes cluster kubeconfig save zaki-prod
      
      - name: Restart deployments
        run: |
          kubectl rollout restart deployment/zaki-api -n zaki
          kubectl rollout restart deployment/zaki-web -n zaki
          kubectl rollout restart deployment/zaki-website -n zaki || true
